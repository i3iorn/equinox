# Dependency source of truth: pyproject.toml
# This file is a convenience entrypoint for environments that prefer:
#   pip install -r requirements.txt
# It intentionally delegates to pyproject extras to avoid manifest drift.

-e .[dev]
