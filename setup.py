"""Compatibility setup.py.

Package metadata and dependencies are defined in pyproject.toml (PEP 621).
This file exists only for tooling that still invokes setup.py directly.
"""

from setuptools import setup


setup()
